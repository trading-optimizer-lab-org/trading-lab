name: GitHub Actions Unblock Smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checkout-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Confirm checkout works
        run: echo checkout_ok
